<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="<%= appearance.description %> | Powered by HolaClient.">
    <meta name="keywords" content="HClient, HolaClient, Hosting, HolaLLC, Freemium Minecraft Hosting, <%= appearance.name %>, Cloud, Minecraft, Servers, Free Servers, Hostings that uses HolaClient">
    <meta name="author" content="HolaClient">
    <meta name="copyright" content="<%= appearance.name %>">
    <meta property="og:type" content="website">
    <meta property="og:title" content="<%= appearance.name %>">
    <meta property="og:description" content="<%= appearance.description %> | Powered by HolaClient.">
    <meta property="og:image" content="<%= appearance.logo?.url || 'https://cdn.holaclientx.tech/logo.png' %>">
    <meta name="twitter:card" content="<%= appearance.logo?.url || 'https://cdn.holaclientx.tech/logo.png' %>">
    <meta name="twitter:title" content="<%= appearance.name %>">
    <meta name="twitter:description" content="<%= appearance.description %> | Powered by HolaClient.">
    <meta name="twitter:image" content="<%= appearance.logo?.url || 'https://cdn.holaclientx.tech/logo.png' %>">
    <meta name="twitter:image:src" content="<%= appearance.logo?.url || 'https://cdn.holaclientx.tech/logo.png' %>">
    <title><%= appearance.name %></title>
    <link rel="shortcut icon" href="<%= appearance.logo?.url || 'https://cdn.holaclientx.tech/logo.png' %>" type="image/x-icon">
    <link rel="icon" type="image/png" href="<%= appearance.logo?.url || 'https://cdn.holaclientx.tech/logo.png' %>">
    <link rel="manifest" href="/manifest.json">
    <meta name="theme-color" content="#18181b">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Nunito&family=Urbanist&family=Quicksand&family=Karla&family=Raleway&family=Inspiration&family=Rajdhani&family=Poppins&display=swap">
    <% if (process.env.APP_ENV !== "production") { %>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="/assets/client/default/app.dev.js"></script>
    <link rel="stylesheet" href="/assets/client/default/app.dev.css">
    <% } else { %>
    <link href="/assets/common/tailwind.css" rel="stylesheet" rel="preload">
    <script src="/assets/client/default/app.js"></script>
    <link rel="stylesheet" href="/assets/client/default/app.css" defer>
    <% } %>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js" defer></script>
    <link rel="stylesheet" href="https://cdn.holaclientx.tech/production/assets/toastr.css">
    <!--
    <link rel="stylesheet"href="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.52.2/codemirror.min.css"></link>
    <script type="text/javascript"src="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.52.2/mode/javascript/javascript.min.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.52.2/theme/monokai.min.css">
    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/codemirror/5.52.2/codemirror.min.js"></script>
    <script type="text/javascript" src="https://cdnjs.cloudflare.com/ajax/libs/jshint/2.11.0/jshint.js"></script>
    -->
</head>
<script>
    if ('serviceWorker' in navigator) {
      window.addEventListener('load', function() {
        navigator.serviceWorker.register('/assets/common/worker.js');
      });
    }
    document.addEventListener("DOMContentLoaded", async function () {
        lazyLoadJS('https://cdn.holaclientx.tech/production/assets/toastr.js')
        lazyLoadJS('https://cdn.jsdelivr.net/npm/sweetalert2@10')
        lazyLoadJS('https://cdn.holaclientx.tech/production/assets/flowbite.js')
        function lazyLoadCSS(a) {
            return new Promise((resolve, reject) => {
                const link = document.createElement('link');
                link.rel = 'stylesheet';
                link.href = a;
                link.onload = resolve;
                link.onerror = reject;
                document.head.appendChild(link);
            });
        };
        function lazyLoadJS(a) {
            return new Promise((resolve, reject) => {
                const script = document.createElement('script');
                script.src = a;
                script.async = true;
                script.onload = resolve;
                script.onerror = reject;
                document.head.appendChild(script);
            });
        };
        setTimeout(() => {
            toastr.options = {
                closeButton: true,
                progressBar: true,
                showHideTransition: 'slide',
                positionClass: 'toast-bottom-right',
                timeOut: 4000,
                extendedTimeOut: 1000,
                showDuration: 300,
                hideDuration: 300,
            };
        }, 2000);
        // let a = document.title;
        // let b = document.body.innerHTML
        // window.addEventListener("blur", () => {
        //     b = document.body.innerHTML
        //     document.body.innerHTML = '<div class="w-screen bg-black h-screen flex justify-center items-center text-center text-white">Window unfocused, disabled frontend processes to reduce resource usage.</div>';
        //  });
        // window.addEventListener("focus", () => {
        //     setTimeout(() => {
        //         document.body.innerHTML = b;
        //         document.title = a;
        //        load()
        //    }, 500);
        //});
    });
</script>